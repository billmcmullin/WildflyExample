<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.example</groupId>
        <artifactId>wildflyexample-parent</artifactId>
        <version>1.0-SNAPSHOT</version>
        <relativePath>..</relativePath>
    </parent>

    <artifactId>selenium-testng-tests</artifactId>
    <packaging>jar</packaging>

    <name>Selenium Browser Tests (TestNG)</name>
    <description>Browser tests configured for Parasoft coverage collection</description>

    <properties>
        <app.base.url>http://localhost:8080/app</app.base.url>
        <app.username>admin</app.username>
        <app.password>admin</app.password>
        <selenium.tests.skip>true</selenium.tests.skip>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.testng</groupId>
            <artifactId>testng</artifactId>
            <version>${testng.version}</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.seleniumhq.selenium</groupId>
            <artifactId>selenium-java</artifactId>
            <version>${selenium.version}</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>com.parasoft</groupId>
            <artifactId>coverage-integration-testng</artifactId>
            <version>${coverage.integration.version}</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>com.parasoft</groupId>
            <artifactId>coverage-integration-selenium</artifactId>
            <version>${coverage.integration.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <artifactId>maven-compiler-plugin</artifactId>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <useModulePath>false</useModulePath>
                    <skipTests>${selenium.tests.skip}</skipTests>
                    <suiteXmlFiles>
                        <suiteXmlFile>src/test/resources/testng.xml</suiteXmlFile>
                    </suiteXmlFiles>
                    <systemPropertyVariables>
                        <app.base.url>${app.base.url}</app.base.url>
                        <app.username>${app.username}</app.username>
                        <app.password>${app.password}</app.password>
                    </systemPropertyVariables>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>run-selenium-tests</id>
            <properties>
                <selenium.tests.skip>false</selenium.tests.skip>
            </properties>
        </profile>
    </profiles>
</project>
